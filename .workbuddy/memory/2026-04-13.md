# 2026-04-13

## CV页面手机端显示问题修复

修复了 cv.html 页面在手机端显示异常的问题，主要修改了 cv.css：

1. **修复移动端导航浮动效果**：
   - 将 `inset` 属性改为明确的 `top`, `left`, `width` 属性，避免浏览器兼容性问题
   - 为 `.mobile-panel` 添加 `position: absolute` 和正确的定位
   - 修复了 `.mobile-panel.active` 的显示逻辑

2. **优化手机端面板显示**：
   - 为 `.intro-panel` 和 `.cv-panel` 添加 `overflow: visible`
   - 调整了手机端的内边距和字体大小

3. **改进响应式排版**：
   - 优化了 `.intro-title` 和 `.cv-heading` 在手机端的字号
   - 调整了 `.section-toggle` 的样式
